{
  "name": "bartek-house-hunt",
  "version": "1.0.0",
  "description": "Telegram Property Tracker and Dashboard for LBS Apartment Hunt",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
